<set xmlns:android="http://schemas.android.com/apk/res/android">
    <alpha
        android:fromAlpha="1.0"
        android:toAlpha="0.0"
        android:duration="1000" /> <!-- Set duration in milliseconds (e.g., 1 second) -->
</set>
